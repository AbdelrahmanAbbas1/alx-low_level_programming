#include "main.h"

/**
 * add - return the sum of the two numbers
 * @a: the first argument
 * @b: the second argument
 * Return: return the sum of the two arguments
 */
int add(int a, int b)
{
	return (a + b);
}
