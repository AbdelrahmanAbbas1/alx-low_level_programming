#include "main.h"

/**
 * _abs - Prints the absolute value of the integer
 * @n: The argument
 * Return: the absloute value of n (success)
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
