#include "3-calc.h"

/**
 * op_add - Adds numbers
 * @a: The first number
 * @b: The seconde number
 *
 * Return: the result of the operation and otherwise if error
 */
int op_add(int a, int b)
{
	return (a + b);
}

/**
 * op_sub - Adds numbers
 * @a: The first number
 * @b: The seconde number
 *
 * Return: the result of the operation and otherwise if error
 */
int op_sub(int a, int b)
{
	return (a - b);
}

/**
 * op_mul - Adds numbers
 * @a: The first number
 * @b: The seconde number
 *
 * Return: the result of the operation and otherwise if error
 */
int op_mul(int a, int b)
{
	return (a * b);
}

/**
 * op_div - Adds numbers
 * @a: The first number
 * @b: The seconde number
 *
 * Return: the result of the operation and otherwise if error
 */
int op_div(int a, int b)
{
	return (a / b);
}

/**
 * op_mod - Adds numbers
 * @a: The first number
 * @b: The seconde number
 *
 * Return: the result of the operation and otherwise if error
 */
int op_mod(int a, int b)
{
	return (a % b);
}
